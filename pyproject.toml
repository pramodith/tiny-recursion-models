[project]
name = "tiny-recursion-models"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "datasets>=4.1.1",
    "lightning>=2.5.5",
    "ruff>=0.14.0",
    "transformers>=4.57.0",
    "wandb>=0.22.2",
]
